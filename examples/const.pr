const PI = 3.14159

let caught = false
try {
    PI = 3
} catch err {
    caught = true
}

assert_eq(caught, true, "assigning to const raises error")
assert_eq(PI, 3.14159, "const value unchanged")

output("[PASS] examples/const")