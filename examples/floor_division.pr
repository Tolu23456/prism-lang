/* Prism example: Floor division (//) and integer arithmetic */

output("=== Floor Division Examples ===\n")

/* Basic floor division */
output("7 // 2   =", 7 // 2)      /* 3 */
output("-7 // 2  =", -7 // 2)     /* -4 (rounds toward -infinity) */
output("7 // -2  =", 7 // -2)     /* -4 */
output("-7 // -2 =", -7 // -2)    /* 3 */
output("10 // 3  =", 10 // 3)     /* 3 */
output("10 // 10 =", 10 // 10)    /* 1 */
output("1 // 2   =", 1 // 2)      /* 0 */
output()

/* Integer quotient and remainder */
func divmod(a, b) {
    let q = a // b
    let r = a - q * b
    return (q, r)
}

let pairs = [(17, 5), (-17, 5), (17, -5), (-17, -5)]
for pair in pairs {
    let q_r = divmod(pair[0], pair[1])
    output(pair[0], "//", pair[1], "=", q_r[0],
          "  remainder:", q_r[1])
}
output()

/* Integer digit extraction */
func digits(n) {
    if n < 0 { n = -n }
    if n == 0 { return [0] }
    let result = []
    while n > 0 {
        let d = n % 10
        push(result, d)
        n = n // 10
    }
    return reverse(result)
}

output("digits(12345) =", digits(12345))
output("digits(1000)  =", digits(1000))
output("digits(7)     =", digits(7))
output()

/* Page calculation */
func page_count(items, per_page) {
    return (items + per_page - 1) // per_page
}

output("100 items, 10 per page:", page_count(100, 10), "pages")
output("101 items, 10 per page:", page_count(101, 10), "pages")
output("99  items, 10 per page:", page_count(99, 10),  "pages")
output()

/* Integer square root */
func isqrt(n) {
    if n < 0 { return -1 }
    if n == 0 { return 0 }
    let x = n
    let y = (x + 1) // 2
    while y < x {
        x = y
        y = (x + n // x) // 2
    }
    return x
}

output("isqrt(16)  =", isqrt(16))    /* 4 */
output("isqrt(17)  =", isqrt(17))    /* 4 */
output("isqrt(100) =", isqrt(100))   /* 10 */
output("isqrt(99)  =", isqrt(99))    /* 9 */
